<%= form_for([@product, @product.comments.build]) do |f| %>
  <div class="form-group">
    <!-- <%= f.label :body, "" %> -->
    <%= f.text_area :body, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit value: "Отправить", class: "btn btn-success pull-right" %>
  </div>
<% end %>
