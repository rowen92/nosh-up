<%= form_for(@admin_spoiled_food) do |f| %>
  <% if @admin_spoiled_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_spoiled_food.errors.count, "error") %> prohibited this admin_spoiled_food from being saved:</h2>

      <ul>
      <% @admin_spoiled_food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
